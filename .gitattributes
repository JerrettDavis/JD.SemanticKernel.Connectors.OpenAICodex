# Normalize all text files to LF in the repo, check out as CRLF on Windows.
# This ensures dotnet format ENDOFLINE checks pass on all platforms since
# .editorconfig specifies end_of_line = crlf for C# sources.
* text=auto eol=crlf

# Shell scripts must stay LF
*.sh text eol=lf
